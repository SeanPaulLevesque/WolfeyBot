# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environment
.venv/
venv/

# Personal/local Claude settings (keep the shared .claude/settings.json)
.claude/settings.local.json

# Local Showdown credentials (never commit)
bot_secrets.py

# Runtime logs (may contain session data / credentials)
*.log
bot.log

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/

# Ad-hoc analysis scratch slot (see CLAUDE.md tools row) -- never committed
tools/scratch.py
# In-project scratch for commit messages / release notes (write access is
# scoped to this project, so these stay inside the repo, gitignored)
tools/scratch/
